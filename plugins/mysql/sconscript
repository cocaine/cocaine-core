#!/usr/bin/scons

Import(['env'])

plugin = env.SharedLibrary(
    target = "#build/plugins/mysql",
    SHLIBPREFIX = "",
    source = ["mysql.cpp"],
    CPPPATH = ["#include"],
    LIBS = ["uriparser", "mysqlclient"],
    LINKFLAGS = ["-Wl,-Bsymbolic"])

Return("plugin")

#!/usr/bin/scons

Import(['env'])

plugin = env.SharedLibrary(
    target = "#build/plugins/inject",
    SHLIBPREFIX = "",
    source = ["inject.cpp"],
    CPPPATH = ["#include"],
    LIBS = ["uriparser", "zmq"],
    LINKFLAGS = ["-Wl,-Bsymbolic"])

Return("plugin")
